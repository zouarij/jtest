def actualRecDate = operation.ReconciliationDate
if (actualRecDate == null) {
    log.warn("ReconciliationDate is null in response (allowed for now).")
    writeToCSV("ZEMBRA Booking Reconciliation", "ReconciliationDate", "WARN", actualRecDate, todayDate)
} else {
    compareFields("ReconciliationDate", actualRecDate, todayDate)
}
